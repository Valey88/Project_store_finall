import React from "react";
import { jwtDecode } from "jwt-decode";
import { useGetOneUserQuery } from "../../redux/dataApi";
import { useParams } from "react-router-dom";

const Account = () => {
  return <div>account</div>;
};

export default Account;
